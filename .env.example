# AI runs in the Python process. The model downloads automatically on first use
# and is reused from the Hugging Face cache on later runs.
HF_MODEL_ID=mrm8488/t5-base-finetuned-question-generation-ap
HF_DEVICE=auto

# Local web server settings
HOST=127.0.0.1
PORT=5000
