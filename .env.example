# AI is selected by default. Set this key to generate reviewed AI cards.
# Alternatively select Local rules in the UI (offline patterns, not a local LLM).
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Local web server settings
HOST=127.0.0.1
PORT=5000
